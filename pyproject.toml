[project]
name = "pakkausalgoritmit"
version = "0.1.0"
description = ""
authors = [
    {name = "kuussant",email = "santeri.kuusisto@helsinki.fi"}
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
]


[tool.poetry]
packages = [
    { include = "lz78", from = "src" },
    { include = "huffman", from = "src" }
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "pytest (>=9.1.1,<10.0.0)"
]
